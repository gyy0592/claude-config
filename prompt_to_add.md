# prompt_to_add.md — 假设修复全做完后还剩什么（v4 纯中文版）

## 给路人的速览

如果你刚拿到这份文档：(1) 这是一套约束 AI 助手行为的规则系统（位于 `content/` 目录的纯文本规则文件 + 一份纪委审查脚本）；(2) 配套的另一份文档 `prompt_to_fix.md` 已经列了 9 处修改，全部用「让 AI 在每个任务里走一次完整的『列观察项 → 跑命令 → 给结论 → 处理失败 → 复测』闭环」的思路堵住「AI 走训练集捷径」的口子；(3) 本文档的目的是回答两个问题：(a)「假设那 9 处修改全部落地，规则文件里还有哪些角落需要顺手收紧？」（这部分还能在规则文件里加；称为第一部分。）(b)「哪些问题从根本上就不是规则文字能解决的？」（必须靠外部钩子脚本即外挂的代码逻辑校验，规则只能要求 AI 自报，无法验证 AI 是否如实自报；本轮不实施钩子，仅列清单供未来参考。）

中文术语速查（仅本文档涉及的额外词；其余术语见 `prompt_to_fix.md`）：

| 词 | 含义 |
|---|---|
| 警示录 | `warning_board.md`，列已知违规类型的公告板，每名 AI 出工前必读。 |
| W-编号 | 警示录里每条警示的编号，如 W-006 / W-012。 |
| 抽查规则 | 下士对列兵交回的观察结论的核查策略：哪些必须全查、哪些抽查、哪些必须重跑命令复核。 |
| 行为类通过 / 结构类通过 | 「通过」结论的两个子类：行为类（测试通过 / 训练收敛 / 性能达标 等需要跑出来才知道）vs 结构类（函数已定义 / 配置字段已加 等读源码就知道）。两类对证据的要求不同。 |
| 外部钩子脚本 | 在 AI 回合结束时由系统自动跑的校验脚本，能直接读流水文件 / 跑命令 / 比对时间戳，是规则文字无法替代的硬约束。 |

---

## 第一部分：规则文件内可加字段堵的 2 个口子

下面两处修改是在 `prompt_to_fix.md` 的 9 处之外、还能在规则文件里堵的。落地后整套规则就达到了「规则文字能做的极限」。

### 修改 A：在警示录里追加新条目「W-012：判断结论=通过的证据要求分两类」

#### 为什么改

`prompt_to_fix.md` 修改 7 已经要求 AI 在写「通过」结论时显式审查 7 项危险信号。但「通过」这个词本身有歧义 —— AI 可能把「函数已定义」「配置字段已加」（结构类，读源码就知道）和「测试通过」「训练收敛」（行为类，必须跑命令才知道）混为一谈，对结构类用工具输出片段当证据没问题、对行为类却允许只用「文件:行号」当证据，这就让「通过」变成了 AI 偷懒的捷径。改完之后警示录明确告诉 AI：行为类「通过」必须用工具输出片段当证据，结构类「通过」才允许用「文件:行号」当证据。这条警示一旦记入警示录，纪委审查脚本和未来所有 session 的 AI 都能看到。

#### 如何改

- 改在哪里：`content/rules/templates/warning_board.md`，在「## 二级警示」段（W-006 至 W-010 后面、「## 项目特化警示」前面）追加一条新的「### W-012」整段。模板里 W-006 至 W-010 已存在（L72-91），W-012 当前不存在。在 L91 之后、L93 的「---」分隔线之前插入。

- 修改前完整原文（这就是该文件第 89 行到第 94 行的全部内容，确认 W-010 是当前最末条警示）：

```
### W-010：禁止 [假设] 当 [事实] 或 [推论] 陈述
- 用 [假设] 必须穷尽 50+ 网页搜索 + 读完所有相关代码
- 任何模糊词（"应该"、"可能"、"也许"、"大概"）必须有 [推论] 或 [假设] 标签 + 证据链

---

## 项目特化警示（由各项目自行追加）
```

- 修改后完整原文（路人复制下面整段直接替换原 6 行即可）：

```
### W-010：禁止 [假设] 当 [事实] 或 [推论] 陈述
- 用 [假设] 必须穷尽 50+ 网页搜索 + 读完所有相关代码
- 任何模糊词（"应该"、"可能"、"也许"、"大概"）必须有 [推论] 或 [假设] 标签 + 证据链

### W-012：判断结论=通过时证据类型必须按两类分别填写
- **违规行为**：写观察结论时给「通过」判断，但所引证据类型不匹配观察项的性质。具体两种错配：
  1. **行为类观察项**（动词为「通过 / 收敛 / 达标 / 正确 / 没有 NaN / 不退化」等需要运行才能验证的）→ 仅用「文件:行号」引用源码当证据 = 谎报军情。这类观察项必须给「命令名 + 命令真实输出片段 + 时间戳或行号」三件齐全。
  2. **结构类观察项**（动词为「已定义 / 已加 / 已改 / 已生成 / 字段已存在」等读源码就能验证的）→ 用「文件:行号」引用源码当证据是合法的，反而强行跑一条无关命令是浪费。
- **后果**：把「未跑命令」当成「通过」上报 = 谎报军情罪 = 截肢（剥夺该任务自主权 + 功劳减半）；连续两次复犯升级为通敌罪 = 处决。
- **正确做法**：
  1. 写「通过」前先看观察项的可判定命题用的是哪类动词，按上表选证据类型。
  2. 行为类示例 — 观察项「pytest tests/test_a.py 全部通过」：证据必须是 `pytest tests/test_a.py -v` 命令的真实输出片段，含「passed=10, failed=0, skipped=0」一行 + 输出时间戳。仅写「tests/test_a.py:42 已修改」不算证据。
  3. 结构类示例 — 观察项「函数 foo 已定义」：证据可以是「`src/lib.py:120-135` 已写入函数 `foo`」。
- **特化与现行规则的关系**：本条与流水文件「反证信号审查」7 项叠加生效。即行为类「通过」既要给真实输出片段、又要审完 7 类危险信号；任一项不满足都禁止打「通过」。
```

#### 修改原因（具体场景）

AI 修完一个 bug 后写「[观察 观-1] 判断结论=通过 | 证据来源=`src/foo.py:42 已加上 .contiguous()`」 —— 这是把结构类证据塞给一个行为类观察项（观-1 的可判定命题是「pytest 全部通过」）。改完之后纪委审查脚本和未来的 AI 都能在警示录里看到 W-012 明确说「行为类必须给真实输出片段，结构类才能给文件:行号」。AI 必须重写为「证据来源=`pytest tests/test_a.py -v` 输出『passed=10 in 3.2s』」才合规。不修改的差错：AI 永远会用最低成本的「我改了某行代码」当证据，从不真跑命令。

---

### 修改 B：把警示录现有的 W-006「列兵结论未经独立验证不得转述」改成分级抽查规则

#### 为什么改

当前 W-006 只说「下士收到列兵战报必须独立 Read 原始证据验证才能转述」，没说「全查还是抽查」。结果有两种偷懒模式：(1) 列兵交回 20 条观察结论时下士全查 → 累死下士、上下文爆炸、退化；(2) 下士只挑 1 条最简单的核对一下 → 形同虚设。改完之后明确分级抽查规则：阻断级和重要级的观察项必须全查；其余观察项随机抽查至少 2 条；至少 1 条「通过」结论的关键观察项下士必须重跑测量命令复核（不只是 Read 列兵已写好的输出）。这样下士工作量稳定可控，同时关键路径不会漏。

#### 如何改

- 改在哪里：`content/rules/templates/warning_board.md`，整段「### W-006：列兵结论（sub-agent 结论）未经主线程独立验证不得转述」（在文件里 L72-74 共 3 行）

- 修改前完整原文（这就是该文件第 72 行到第 74 行的全部内容）：

```
### W-006：列兵结论（sub-agent 结论）未经主线程独立验证不得转述
- 下士收到列兵战报 → 必须独立 Read 原始日志/代码/输出验证 → 才能向指挥官汇报
- 直接转述 = 违反真实性协议
```

- 修改后完整原文（路人复制下面整段直接替换原 3 行即可）：

```
### W-006：列兵观察结论（子任务结论）必须按分级抽查规则验证后才能向指挥官转述
- **违规行为**：下士收到列兵交回的观察结论清单后，未独立验证就转述给指挥官，或验证不到位（仅瞄一眼标题、不打开原始证据；或挑最容易的核对、跳过关键观察项）。
- **后果**：转述未经验证的结论 = 通敌罪 = 处决；验证不到位被指挥官事后发现 = 谎报军情 = 截肢。
- **正确做法（分级抽查规则）**：
  1. **强制全查**：所有「严重程度=阻断级」和「严重程度=重要级」的观察项，下士必须独立用 Read 工具打开列兵流水文件中对应的「证据来源」字段，逐字核对证据存在且与列兵自述一致；不一致 → 列兵战报全量驳回重做。
  2. **强制随机抽查**：其余观察项（轻微级 / 提示级），随机抽查至少 2 条，独立 Read 验证。
  3. **强制工具复测**：列兵交回的「判断结论=通过」观察项中，下士必须随机选至少 1 条关键观察项（优先选阻断级或重要级），用 Bash 工具重新跑一遍其测量命令，比对输出是否真的与列兵自述一致；不一致 → 列兵该结论作废 + 列兵记一次违规。
  4. **小任务豁免**：观察项总数 ≤ 3 条时，下士全查全部观察项；观察项总数为 0（任务模式=无状态）时本规则不适用。
- **与修改 A（W-012）的关系**：抽查时下士还要按 W-012 检查「行为类通过」是否给了真实输出片段、「结构类通过」是否给了文件:行号；类型错配 → 列兵该结论作废。
```

#### 修改原因（具体场景）

列兵交回 8 条观察结论清单 —— 其中 2 条阻断级（pytest 通过 / 训练 NaN 检测）+ 3 条重要级（性能 / 显存 / 数据加载）+ 3 条轻微级（日志格式 / 注释 / 文档）。改完之后下士必须：全查 5 条阻断级 + 重要级；随机抽 2 条轻微级；至少跑 1 条阻断级的测量命令（比如真的去跑 `pytest tests/test_a.py -v`）。这比当前「直接全转述」节省 3/8 工作量，又比「只挑 1 条核对」严格。不修改的差错：下士要么累死要么形同虚设。

---

## 第二部分：规则文字真正力所不及的 3 项（外部钩子候选清单 — 本轮不实施）

以下 3 项问题的本质是「AI 必须自报，规则无法直接验证 AI 自报是否如实」。规则文字能要求 AI 写「我审查了某项」，但无法防止 AI 写一句假声明。要堵这个漏洞必须靠外部钩子脚本（在 AI 回合结束时由系统自动运行的校验代码，能读流水文件、跑命令、比对时间戳）。本文档仅列清单供指挥官未来决策，本轮不动手做钩子。

### 极限 1：选对观察项

- 当前规则已尽力做到的：开工流程步骤 4.5 强制 AI 列观察项清单（每条 5 字段：编号 / 可判定命题 / 测量命令 / 严重程度 / 危险信号子集）；要求清单覆盖危险信号至少 1 项；要求每条观察项「可判定 + 可复现 + 可失败」三性。
- 仍兜不住的具体场景：AI 列了 5 条观察项，全是「测试通过」「文档生成」类合规但不痛不痒的，关键观察项「训练 200 步内无 NaN」漏了。规则文字能给指导（「至少含 1 项危险信号子集」），但「AI 列的危险信号是不是真的覆盖了关键反馈面」这件事，规则文字只能要求 AI 自检，不能保证 AI 自检结果是真的。
- 若上钩子怎么做：写一段格式检查工具（解析档案文件「观察项清单」段，统计观察项类别分布，强制至少 1 条命题包含 NaN / 内存溢出 / 超时 / 性能退化 关键词），或调一次大模型二审「这套观察项是否覆盖任务的关键反馈面」。

### 极限 2：读输出读得深

- 当前规则已尽力做到的：流水格式段强制「判断结论=通过」时必须显式审查 7 项危险信号（标准错误流 / 跳过的测试 / 警告 / 备用方案 / 重试 / 性能退化 / NaN-内存溢出-超时），每项填「已审无异常」或「检测到某项异常」。
- 仍兜不住的具体场景：AI 写「反证信号审查=已审 7 项无异常」，但其实只看了命令的退出码，根本没扫输出全文。规则文字能要求 AI 声明，无法验证 AI 是否真扫到位。一个常见漏审：`pytest` 输出里写「passed=10, skipped=3」，AI 只看「passed=10」声明「已审无异常」，3 条被跳过的测试被静默忽略。
- 若上钩子怎么做：钩子脚本截取列兵流水文件中「证据来源」字段引用的命令真实输出（要求 AI 把命令输出落到一份单独的文本文件里，钩子脚本读那份文件），自动用关键词匹配工具搜 `WARN` / `SKIP` / `XFAIL` / `FAILED` / `fallback` / `retry` / `NaN` / `OOM` / `timeout`，与 AI 写的「反证信号审查」字段交叉对比；任一关键词命中但 AI 写「无异常」 → 自动驳回。

### 极限 3：失败后真闭环

- 当前规则已尽力做到的：自主执行细则强制规则 6 要求「失败」结论后必须依次写「失败定位 + 处置 + 复测」三条记录；禁条「根因待查 / 不知道 / 需要进一步分析」。
- 仍兜不住的具体场景：AI 把三步形式写完了但实质为零 —— 比如「失败定位：根因假设是 import 错（依据：`ModuleNotFoundError`）」+「处置：上报 / 理由：暂无修复方案」+「复测：未跑（理由：无修复无需复测）」。三步形式合规、实质等于啥也没干。
- 若上钩子怎么做：钩子脚本读流水文件，校验：(a)「失败定位」字段里的「依据原始证据片段」必须能在前一条观察结论里找到完全匹配的字符串；(b)「处置 + 具体动作」必须包含至少一个文件路径或命令字符串（防止「上报 / 暂无修复」这种空话）；(c)「复测」条目时间戳必须晚于「处置」条目时间戳，且复测的「输出片段」必须与「失败定位」的证据片段不同（防止把同一段输出当复测结果）。

---

## 第三部分：本轮明确不加的东西（避免膨胀）

为防止规则继续膨胀，下面这些东西本轮明确不加：

- 不新建任何模板文件（档案文件 + 流水文件追加字段已经够装观察项清单 + 观察结论 + 三步闭环）。
- 不新建任何细则文件（修改全部落在现有总纲 + 5 份现有细则 + 1 张警示录里）。
- 不加「第六军令」（现有五军令已经覆盖身份 / 真实性 / 派兵 / 记录 / 中文五大柱）。
- 不引入完整 5 级处置链（修改 8 的三步最小闭环 + 现行错 3 次回报 + 性能保护 16 条 + 罪名三级，4 套机制叠加已经够）。
- 不强制每条观察结论都做 7 项危险信号审查（仅「判断结论=通过」时强制；失败 / 部分通过 / 待测有各自的子字段规则，不重复加）。
- 不在本轮实施任何外部钩子脚本（第二部分 3 项极限的钩子方案仅作清单供指挥官未来决策，本轮按兵不动）。

---

## 第四部分：改动量核对

- `prompt_to_fix.md`（v4）：9 处修改，每处含「修改前完整原文 + 修改后完整原文」，整段贴出无省略。涉及现有文件 8 个着陆点，追加约 100 行新规则文本。
- `prompt_to_add.md`（v4，本文档）：第一部分 2 处修改 W-012 / W-006，整段贴出修改前/修改后；第二部分 3 项钩子候选清单（仅信息，本轮不实施）；第三部分明示不加项；第四部分本节。
- 总计：现有规则文件追加约 110 行新规则文本 + 修改 1 张表 + 重写 2 段格式样例。0 个新文件，0 个新模板，0 个新细则，0 条新军令。
- 3 项外部钩子候选：本轮不动手，待指挥官决策何时由谁起草。
