# Y号列兵派遣令

**编号**：Y号列兵
**所属下士**：X号下士
**派出时间**：（到岗后填写：YYYY-MM-DD HH:MM UTC）
**状态**：DEPLOYED

---

## 授权字段

<!-- 默认：无（每步必须请示）。
     下士在 Agent prompt 中明确写明自主权时，列兵到岗后在此填写授权范围。
-->

授权字段：（到岗后填写 — 从 Agent prompt 中复制授权字段内容）

---

## Agent prompt 原文（列兵到岗后自行粘贴收到的 prompt 全文）

<!-- 列兵到岗第一步：
     1. 调用 init_soldier.sh 生成本文件（若尚未调用）
     2. 将收到的 Agent prompt 全文原封不动粘贴到下方代码块
     3. 填写派出时间和授权字段
     4. 然后才能开始任务
-->

```
（在此粘贴收到的 Agent prompt 全文，一字不差）
```

---

## 最终状态

<!-- ⚠️ 状态更新铁律：任务完成时，soldier_action.md 最后一条写完后的同一回复内，
     必须把「状态」字段改为 COMPLETED。
     状态仍为 DEPLOYED = 任务视为未完成 = 功劳不计。
-->

- 完成时间：（完成后填写）
- 状态：DEPLOYED → 任务完成后立刻改为 COMPLETED / EXECUTED（处决）
- 原因：（做对了什么 / 哪里叛国）
- 警告：（对未来列兵的教训）
