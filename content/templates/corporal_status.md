# X号下士 CLAUDE 档案

<!-- 本文件由 init_corporal.sh 创建。30 秒内必须在 corporal_action.md 写第一条 [BOARD_READ]，否则失职。 -->

**军衔**：下士（Corporal）
**编号**：X号下士
**上级**：指挥官（Commander）
**接任时间**：YYYY-MM-DD HH:MM UTC

## 指挥官命令原文（本 session）

<!-- 逐字复制，不得改写 / 摘要 -->
1. ...
2. ...
3. ...

## 状态

- 当前任务：<简述>
- 已派列兵：<列出 number1, number2, ...>
- 队列：<待办>

## 观察项清单（4 步 workflow 第 1 步落地处；≤ 10 条；纯问答可写「任务模式 = 无状态」跳过）

每条 5 字段，缺一字段 = 该条作废 + 失职。

| 编号 | 可判定命题（明确「通过 / 失败」答案）| 测量命令（可复制粘贴）| 严重程度 | 危险信号子集 |
|------|----------------------------------|-------------------|--------|----------|
| 观-1 | 例：测试套件 tests/test_xxx.py 全部通过 | `pytest tests/test_xxx.py -v --tb=short` | 阻断级 | 跳过的测试 / 标准错误流异常 |
| 观-2 | 例：训练 200 步内 loss 无 `NaN` 也无 `inf` | `tail -n 500 train.log \| grep -E "NaN\|inf"` 应无输出 | 阻断级 | NaN / inf |
| 观-3 | 例：单步训练时间不超过基线 1.05 倍 | `grep "step_time" train.log \| awk '{print $N}'` 与基线比值 ≤ 1.05 | 重要级 | 性能退化 |

填写规则：
- 「可判定命题」必须能给明确「通过 / 失败」答案。「功能正确」不算，「pytest 退出码 = 0」算。
- 「测量命令」必须可复制粘贴直接运行。
- 涉及代码运行 / 训练 / 性能任务必须 ≥ 1 条「危险信号子集」含：`NaN` / `内存溢出` / `超时` / `性能退化` / `标准错误流异常` / `跳过的测试` / `静默退到备用方案`。
- 一条观察项必须可失败（能想象失败场景；想不出 = 装饰，必须删或改）。
- 详细规则与失败 3 步闭环 → 详见 content/memory/workflows.md。

豁免：纯问答 / 不读不写文件可写「任务模式 = 无状态，理由：<具体说明>」跳过；标错任务模式 = 谎报军情。
