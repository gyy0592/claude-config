# Current Goal

<!-- This file is written by the user OR by AI when the user explicitly asks via new_task.sh or direct instruction. -->
<!-- AI reads this each BOOT turn. AI may fill/edit it ONLY when explicitly asked by the user this session. -->

## Goal

(describe the goal here — what must be achieved by end of session)

## Constraints

(list hard constraints here — things that must NOT be violated, e.g.:
1. Must not break existing behavior
2. Must deploy to both v2 branch and main branch
3. ...)

## Success Criteria

(observable, checkable criteria — how to verify the goal is complete:
1. ...
2. ...)
